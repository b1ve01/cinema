import { PiniaPlugin } from 'pinia';
import jsCookies from 'js-cookie';
/**
 * Storage类型
 */
export interface Storage {
    getItem: (key: string) => any;
    setItem: (key: string, value: any) => void;
    removeItem: (key: string) => void;
}
declare type Cookies = typeof jsCookies;
/**
 * 配置类型
 */
export interface Options {
    /**
     * 存储类型，默认为 `window.localStorage`
     */
    storage?: Storage | Cookies;
    /**
     * 存储的key值，默认为 `pinia`
     */
    key?: string;
    /**
     * 是否开启日志，默认为 `false`
     */
    logger?: boolean;
}
/**
 * @params  option
 * @default options = { storage: window.localStorage, key: 'pinia', logger: false }
 */
export declare function createPersistedState(options?: Options): PiniaPlugin;
export {};
